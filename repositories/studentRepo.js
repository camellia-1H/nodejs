const getAllStudents = async ({ page, size, searchString }) => {
  console.log("get all student  repo");
};

const insertStudent = async ({
  name,
  email,
  languages,
  gender,
  phoneNumber,
  address,
}) => {
  console.log("insert studen repo");
};

export default { getAllStudents, insertStudent };
